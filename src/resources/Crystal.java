package resources;

import java.awt.*;

public class Crystal extends Resource {
    public Crystal(Integer amount, Integer positionX, Integer positionY, Integer width, Integer height,
                Boolean doesExists, Image image)
    {
        super(amount, positionX, positionY, width, height, doesExists, image);
    }
}